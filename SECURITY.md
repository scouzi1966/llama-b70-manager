# Security

This is a local-only manager for a llama.cpp server process.

Please report security issues by opening a GitHub issue with enough detail to reproduce the problem. Do not include private tokens, API keys, model license credentials, or machine-specific secrets in issues.

The app should only delete `.gguf` files inside the configured Hugging Face cache. Any behavior outside that boundary should be treated as a bug.
